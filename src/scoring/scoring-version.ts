/**
 * cookedPrompts — Scoring Version
 *
 * Bump when rules, weights, labels, thresholds, or the output shape change.
 * Recorded on every PromptScore so results remain interpretable over time.
 */

export const SCORING_VERSION = '1.0.0';
